// chapter 1 practice set
#include <stdio.h>
int main(){

    /* 
    question 1:  Write a C program to calculate area of a rectangle:
        a. Using hard coded inputs.
        b. Using inputs supplied by the user.
    */ 
    // a. hard coded input
    // float len = 4;
    // float bred = 5;
    // float area = len * bred;
    // printf("The area of 4 by 5 rectangle is %0.2f",area);

    // b. input from user
    // float len;
    // float bred;
    // printf("Length: ");
    // scanf("%f",&len);
    // printf("Breadth: ");
    // scanf("%f",&bred);
    // float area = len * bred;
    // printf("The area of %0.1f by %0.1f rectangle is %0.2f",len,bred,area);

    /*
    Question 2 Calculate the area of a circle and modify the same program to calculate the volume of a cylinder given its radius and height.
    */ 
//    float radius, height;
//    printf("Radius: ");
//    scanf("%f",&radius);

//    float circle_area = 3.14 * radius * radius;

//    printf("Height: ");
//    scanf("%f",&height);
//    float cylinder_area = circle_area * height;

//    printf("The area of circle with radius %0.1f is %0.1f \n",radius,circle_area);
//    printf("The area of cylinder with base radius %0.1f and height %0.1f is %0.1f",radius,height,cylinder_area);
    
    /*
    Question 3:  Write a program to convert Celsius (Centigrade degrees temperature to Fahrenheit).
    */
    /*
    Question 4:  Write a program to calculate simple interest for a set of values representing principal, number of years and rate of interest.
    */

}